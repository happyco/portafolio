Objective of the script

Filter the csv of input with the filters on the script, the user can modify the filters according  to their necesity, this will help to create a easy csv file to read


How to use

after running another tool that generate the csv file, run this script with the csv files as a parameter of the script, it will generate a result.csv file

e.g.

	python selftest.py <csv file>.csv